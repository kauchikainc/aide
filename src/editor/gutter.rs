//! 行番号・診断アイコン表示
//!
//! エディター左側のガター領域を管理する。

/// ガターの構造体
pub struct Gutter {
    // TODO: Phase 2以降で実装
}

impl Gutter {
    /// 新しいガターを作成する
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Gutter {
    fn default() -> Self {
        Self::new()
    }
}

//! メインエディタービュー
//!
//! テキストエディタのメイン描画ロジックを提供する。

/// エディタービューの構造体
pub struct EditorView {
    // TODO: Phase 2以降で実装
}

impl EditorView {
    /// 新しいエディタービューを作成する
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for EditorView {
    fn default() -> Self {
        Self::new()
    }
}
